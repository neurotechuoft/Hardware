name: C/C++ CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: make mcp3008lsl
      run: cd ./mcp3008lsl; make
# Enable if tests are written
#    - name: make check
#      run: make check
